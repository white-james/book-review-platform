# NOTE: These are placeholder secrets for local development only.
# For production, use Azure Key Vault + External Secrets Operator
# See: https://external-secrets.io/

apiVersion: v1
kind: Secret
metadata:
  name: book-review-secrets
  namespace: book-review-platform
type: Opaque
stringData:
  POSTGRES_PASSWORD: "password"  # Replace with actual value or use External Secrets
  DB_PASSWORD: "password"        # Replace with actual value or use External Secrets
  JWT_SECRET: "your-super-secret-jwt-key-change-this-in-production"         # Replace with actual value or use External Secrets