# This is a TEMPLATE file - do not store actual secrets here
# Secrets are injected from Azure DevOps pipeline variables
# 
# To apply locally for testing, copy this file to secrets.local.yaml
# and add your actual values, then add secrets.local.yaml to .gitignore
apiVersion: v1
kind: Secret
metadata:
  name: book-review-secrets
  namespace: book-review-platform
type: Opaque
stringData:
  POSTGRES_PASSWORD: "__POSTGRES_PASSWORD__"
  DB_PASSWORD: "__DB_PASSWORD__"
  JWT_SECRET: "__JWT_SECRET__"