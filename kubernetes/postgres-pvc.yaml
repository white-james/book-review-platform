apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: book-review-platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi  # Azure Disk with CSI driver
  resources:
    requests:
      storage: 10Gi