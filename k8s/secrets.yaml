# NOTE: These are placeholder secrets for local development only.
# For production, use Azure Key Vault + External Secrets Operator
# See: https://external-secrets.io/

apiVersion: v1
kind: Secret
metadata:
  name: book-review-secrets
  namespace: book-review-platform
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"  # Replace with actual value or use External Secrets
  DB_PASSWORD: "changeme"        # Replace with actual value or use External Secrets
  JWT_SECRET: "changeme"         # Replace with actual value or use External Secrets
